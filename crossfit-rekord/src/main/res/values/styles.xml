<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Основная тема приложения -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:buttonStyle">@style/MainStyleButton</item>
        <item name="buttonStyle">@style/MainStyleButton</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Стили для Nav bar -->
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" >
        <item name="android:textColorPrimary">@color/colorWhite</item>
    </style>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <!-- Стиль кнопок приложения -->
    <style name="MainStyleButton" parent="@android:style/Widget.Button">
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/button_text_size</item>
        <item name="android:background">@drawable/button_start_screen</item>
        <item name="android:textAppearance">?android:attr/textAppearanceSmallInverse</item>
        <item name="android:gravity">center_vertical|center_horizontal</item>
    </style>

    <style name="MainTextStyle">
        <item name="android:textColor">@color/colorRedPrimary</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!-- Стиль кнопок для экрана с расписанием тренировок -->
    <style name="TableButtonStyle" parent="MainStyleButton">
        <item name="android:background">@drawable/button_schedule</item>
        <item name="android:textColor">@drawable/button_text_color</item>
    </style>

    <!-- Блок отсутствия сети-->
    <!-- Стиль кнопки-->
    <style name="ErrorButtonStyle" parent="MainStyleButton">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:text">@string/error_connection_button</item>
        <item name="android:textColor">@color/colorRedPrimary</item>
        <item name="android:layout_marginTop">15dp</item>
    </style>

    <style name="SelectTrainingButtonStyle" parent="AppTheme">
        <item name="android:background">@drawable/button_select_training</item>
        <item name="android:textColor">@drawable/button_text_color</item>
    </style>

    <!-- Стиль текста-->
    <style name="ErrorTextStyle">
        <item name="android:gravity">center_horizontal</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:textColor">@color/colorRedPrimary</item>
    </style>

    <!-- Стиль календаря -->
    <!-- Стиль кнопок для экрана с расписанием тренировок -->
    <style name="CalendarStyle" parent="AppTheme">
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:typeface">sans</item>
        <item name="android:textColor">@color/buttons_for_calendar_style</item>
    </style>

    <style name="calendarHeaderStyle" parent="AppTheme">
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:typeface">sans</item>
        <item name="android:textColor">@color/colorRedPrimary</item>
    </style>

    <style name="calendarWeekDayStyle" parent="AppTheme">
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:typeface">sans</item>
        <item name="android:textColor">@color/colorRedPrimary</item>
    </style>

    <style name="calendarSelectDayStyle" parent="AppTheme">
        <item name="android:background">@color/colorRedPrimary</item>
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

    <style name="Theme.App.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorControlNormal">@color/colorRedDark</item>
        <item name="colorControlActivated">@color/colorWhite</item>
        <item name="colorControlHighlight">@color/colorPrimary</item>
    </style>

    <style name="Theme.App.Base.MyResult" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorControlNormal">@color/colorRedDark</item>
        <item name="colorControlActivated">@color/colorRedPrimary</item>
        <item name="colorControlHighlight">@color/colorPrimary</item>
    </style>

    <style name="AppTheme.Launcher">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

    <style name="MyRadioButton" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorControlNormal">@color/colorRedDark</item>
        <item name="colorControlActivated">@color/colorRedPrimary</item>
    </style>

    <!-- Стиль табов на экране деталей тренировки-->
    <style name="Base.Widget.Design.TabLayout" parent="android:Widget" tools:ignore="PrivateResource">
        <item name="tabBackground">@drawable/tab_color_selector</item>
        <item name="tabIndicatorColor">@color/colorRedPrimary</item>
        <item name="tabIndicatorHeight">4dp</item>
        <item name="tabSelectedTextColor">@color/colorRedPrimary</item>
        <item name="tabTextColor">@color/colorWhite</item>
    </style>


</resources>